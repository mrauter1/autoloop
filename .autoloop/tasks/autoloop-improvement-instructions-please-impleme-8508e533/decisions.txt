<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260326T141433Z-5dc0310f" ts="2026-03-26T14:14:33.301005+00:00" />
Installer reruns should not mutate existing launcher or skill artifacts unless an explicit overwrite flag is passed, and deleting an existing venv requires a separate explicit recreate flag.
Installer readiness messaging should classify success as installed and ready only when installation succeeded and at least one supported provider CLI is already available; missing provider CLIs remain non-fatal and must include exact next steps.
Skill installation should stay backward-compatible by defaulting to both Codex and Agents targets, while allowing operators to narrow the target set explicitly.
Git flag cleanup should remove only the undocumented --no-no-git alias; config-to-CLI precedence for runtime.no_git must remain unchanged.
<autoloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="2" run_id="run-20260326T141433Z-5dc0310f" ts="2026-03-26T14:17:43.904381+00:00" />
Superseding earlier readiness wording: installer ready/not-ready status must be anchored to the default Codex first-run path, not to the presence of any supported provider CLI.
Claude support remains non-blocking and optional in installer messaging, but Claude-only environments should still be reported as installed but not ready unless the output explicitly directs the operator to switch provider.name to claude and verify claude auth.
<autoloop-decisions-header version="1" block_seq="3" owner="implementer" phase_id="ux-installer-docs-and-cli-hardening" pair="implement" turn_seq="1" run_id="run-20260326T141433Z-5dc0310f" ts="2026-03-26T14:19:06.806274+00:00" />
Installer readiness is reported as ready only when the default Codex path is usable after install, which requires both a detectable codex CLI and the chosen launcher bin directory already being on PATH; otherwise the installer succeeds with exact remediation steps.
Installer overwrite protection is scoped to the managed launcher file, packaged skill SKILL.md targets, and the virtualenv path, with virtualenv deletion remaining separately gated behind --recreate-venv even when --overwrite is passed.
<autoloop-decisions-header version="1" block_seq="4" owner="test_author" phase_id="ux-installer-docs-and-cli-hardening" pair="test" turn_seq="1" run_id="run-20260326T141433Z-5dc0310f" ts="2026-03-26T14:25:45.040102+00:00" />
Installer tests should cover both the guarded failure paths and the explicit rerun success path (`--overwrite` plus `--recreate-venv`), and should encode the PATH-remediation branch that keeps Codex-present installs in the not-ready state when the launcher directory is still absent from PATH.
