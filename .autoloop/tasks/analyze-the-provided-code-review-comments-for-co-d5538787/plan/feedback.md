# Plan ↔ Plan Verifier Feedback

- Added a single-phase plan that treats the three installer comments as likely stale based on current code, requires branch-by-branch re-verification before any edit, and constrains validation to installer plus git-flag/resource coverage.
- PLAN-001 | blocking | The validation plan does not cover the three installer failure branches under review (`require_cmd`, Python version, required repository path). `tests/test_installer.py` exercises installer success/rerun/readiness flows, but it does not execute those specific error paths, so the implementation could declare the comments non-actionable without targeted runtime evidence for the reviewed behavior. Minimal correction: extend the validation plan to include focused subprocess/manual checks or dedicated tests for those three branches in addition to the existing parser/resource coverage.
- Updated the plan to address PLAN-001 by requiring branch-specific installer subprocess coverage for the three reviewed failure paths in `tests/test_installer.py`, while keeping the existing git-flag and resource validation unchanged.
- PLAN-001 | non-blocking | Resolved in the current revision. The plan now requires runtime validation for the `require_cmd`, Python version, and required-path failure branches via the existing installer subprocess harness, and it still preserves the requested `--git` / `--no-git` and resource checks.
